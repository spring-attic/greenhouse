<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:mail="http://www.springframework.org/schema/integration/mail"
	xmlns="http://www.springframework.org/schema/integration" xmlns:task="http://www.springframework.org/schema/task"
	xsi:schemaLocation="http://www.springframework.org/schema/integration http://www.springframework.org/schema/integration/spring-integration-1.0.xsd
		http://www.springframework.org/schema/integration/mail http://www.springframework.org/schema/integration/mail/spring-integration-mail-1.0.xsd
		http://www.springframework.org/schema/task http://www.springframework.org/schema/task/spring-task-3.0.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd">

	<!-- Gateway that publishes signup messages to the global message channel -->
	<gateway id="signupMessageGateway" service-interface="com.springsource.greenhouse.signup.SignedUpMessageGateway" default-request-channel="globalChannel" />
		 		
	<!-- Routes SignupMessages to the signupChannel -->
	<payload-type-router input-channel="globalChannel">
		<mapping type="com.springsource.greenhouse.account.Account" channel="signupChannel" />
	</payload-type-router>	

	<publish-subscribe-channel id="signupChannel" />

	<service-activator input-channel="signupChannel" method="insertSignupUpdate" ref="updatesService" />
		
	<bridge input-channel="signupChannel" output-channel="welcomeMailerChannel" />
	
	<!-- Asynchronously dispatches welcome emails -->
	<channel id="welcomeMailerChannel">
		<dispatcher task-executor="welcomeMailer" />
	</channel>

	<!-- Mailer thread pool -->
	<task:executor id="welcomeMailer" />

	<!-- Transforms a SignupMessage to a welcome MailMessage -->
	<transformer input-channel="welcomeMailerChannel" output-channel="welcomeMail">
		<beans:bean class="com.springsource.greenhouse.account.WelcomeMailTransformer" />
	</transformer>

	<!-- Sends the welcome MailMessage -->
	<mail:outbound-channel-adapter id="welcomeMail" mail-sender="mailSender" />
	
</beans:beans>