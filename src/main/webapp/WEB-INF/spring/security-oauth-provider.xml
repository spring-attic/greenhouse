<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:oauth="http://spring-security-oauth.codehaus.org/3.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
		http://spring-security-oauth.codehaus.org/3.0 http://spring-security-oauth.codehaus.org/schema/spring-security-oauth-3.0.xsd">

	<!-- Steps of OAuth 1.0
		POST /oauth/request_token?oauth_consumer_key&oauth_callback (returns unauthorized token)
		GET /oauth/confirm_access?oauth_token (returns secure authorization form)
		(User) POST /oauth/authorize?requestToken&callbackUrl (authorizes oauth token, redirect to callbackUrl)
		POST /oauth/access_token?oauth_consumer_key&oauth_token&oauth_verifier
	 -->
	 
	<!-- Spring Security OAuth 1.0 Provider Configuration -->	
	<oauth:provider consumer-details-service-ref="consumerDetails"
		token-services-ref="tokenServices"
		request-token-url="/oauth/request_token"
		authenticate-token-url="/oauth/authorize"
		authentication-failed-url="/oauth/confirm_access"
		access-token-url="/oauth/access_token"
		require10a="false" />

	<bean class="org.springframework.security.oauth.provider.OAuthProcessingFilterEntryPoint">
		<property name="realmName" value="Greenhouse" />
	</bean>

	<!-- Adapter that loads ConsumerDetails using an AppRepository -->			
	<bean id="consumerDetails" class="com.springsource.greenhouse.oauth.provider.AppConsumerDetailsService">
		<constructor-arg>
			<bean class="com.springsource.greenhouse.develop.JdbcAppRepository" />
		</constructor-arg>
	</bean>
	
	<!-- Adapter that uses a OAuthSessionManager to manage OAuthSession storage -->
	<bean id="tokenServices" class="com.springsource.greenhouse.oauth.provider.OAuthSessionManagerProviderTokenServices">
		<constructor-arg>
			<!-- Stores OAuthSessions in a ConcurrentMap with soft values and 2 minutes time idle -->
			<bean class="com.springsource.greenhouse.oauth.provider.ConcurrentMapOAuthSessionManager" />
		</constructor-arg>
	</bean>
    	
</beans>